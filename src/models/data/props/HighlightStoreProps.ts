import { HighlightCallbackStore, HighlightValueStore } from "@/models/store/HighlightStore";

export interface HighlightCallbackStoreProps {
	readonly highlightCallbackStore: HighlightCallbackStore;
}

export interface HighlightValueStoreProps {
	readonly highlightValueStore: HighlightValueStore;
}
